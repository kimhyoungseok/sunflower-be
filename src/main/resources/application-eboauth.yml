app:
  auth:
    tokenSecret: ${app.auth.tokenSecret}
    tokenExpiry: ${app.auth.tokenExpiry}
    refreshTokenExpiry: ${app.auth.refreshTokenExpiry}
    refreshTokenSecret: ${app.auth.refreshTokenSecret}